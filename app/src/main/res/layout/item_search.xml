<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="searchItemViewModel"
            type="com.godaddy.namesearch.search_screen.SearchItemViewModel" />
    </data>
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:onClick="@{(view) -> searchItemViewModel.onItemClicked(view)}"
    android:padding="16dp">
    
    <TextView
        android:id="@+id/name_text_view"
        android:text="@{searchItemViewModel.name}"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true" />
    
    <TextView
        android:id="@+id/price_text_view"
        android:text="@{searchItemViewModel.price}"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_alignParentEnd="true"
        />
</RelativeLayout>
</layout>